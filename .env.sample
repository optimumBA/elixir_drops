AWS_ACCESS_KEY_ID=
AWS_ENDPOINT_URL_S3=
AWS_REGION=
AWS_SECRET_ACCESS_KEY=
BUCKET_NAME=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Comma-separated GitHub usernames whose sponsor impressions/clicks are
# excluded from tracking (e.g. team members doing QA on sponsor placements).
# Defaults to empty (no usernames excluded) when unset.
SPONSOR_QA_GITHUB_USERNAMES=
